<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% if @task.persisted?() %>
    <%= f.input :completed, as: :boolean %>
  <% end %>
  <%= f.submit %>
<% end %>
